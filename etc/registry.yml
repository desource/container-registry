version: 0.1

log:
  level: info
  
storage:
  filesystem:
    rootdirectory: /data
  maintenance:
    uploadpurging:
      enabled: true
      age: 168h      # expire after 7 days
      interval: 24h
      dryrun: false
        
http:
    addr: :5000
